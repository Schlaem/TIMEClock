<UserControl x:Class="TIMEClock.UI.WPF.Views.Controls.RecordsOverview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TIMEClock.UI.WPF.Views.Controls"
             xmlns:vms="clr-namespace:TIMEClock.UI.WPF.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance vms:RecordsOverviewViewModel, IsDesignTimeCreatable=False}">

  <DockPanel LastChildFill="True">
    <StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
      <StackPanel.Resources>
        <Style TargetType="{x:Type Button}">
          <Setter Property="Padding" Value="4" />
          <Setter Property="Cursor" Value="Hand" />
        </Style>
      </StackPanel.Resources>
      <Button Content="Refresh" Command="{Binding LoadDataCommand}" />
      <Button Content="Save" Command="{Binding SaveChangesCommand}" />
    </StackPanel>
    
    <DataGrid ItemsSource="{Binding TimeRecordsCollection}"
              CanUserAddRows="{Binding CanAddNewRecord}"
              CellEditEnding="DataGrid_CellEditEnding"
              AutoGenerateColumns="False">
      <DataGrid.Columns>
        <DataGridTextColumn Header="Von" Binding="{Binding From, ConverterCulture='de-CH'}" />
        <DataGridTextColumn Header="Bis" Binding="{Binding Until, ConverterCulture='de-CH'}" />
      </DataGrid.Columns>
    </DataGrid>
  </DockPanel>
</UserControl>
