<UserControl x:Class="TIMEClock.UI.WPF.Views.Controls.SettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TIMEClock.UI.WPF.Views.Controls"
             xmlns:vms="clr-namespace:TIMEClock.UI.WPF.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance vms:SettingsViewModel, IsDesignTimeCreatable=False}">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto" />
      <ColumnDefinition Width="40" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <Label Content="Datenbank" 
           Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="0" />

    <Label Content="Verbindung" Grid.Column="0" Grid.Row="1" />
    <Grid Grid.Row="1" Grid.Column="2">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <TextBox Text="{Binding DbConnectionString, Mode=OneWay}"
               VerticalContentAlignment="Center" Padding="4"
               Grid.Column="0" IsReadOnly="True" />
      <Button Content="S"
              Grid.Column="1"
              MinWidth="{Binding RelativeSource={RelativeSource Self}, Path=ActualHeight}"
              Margin="4,0,0,0"
              Cursor="Hand" />
    </Grid>
  </Grid>
</UserControl>
