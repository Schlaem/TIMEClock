<UserControl x:Class="TIMEClock.UI.WPF.Views.Controls.DashboardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TIMEClock.UI.WPF.Views.Controls"
             xmlns:vms="clr-namespace:TIMEClock.UI.WPF.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance vms:DashboardViewModel, IsDesignTimeCreatable=False}">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <Label Content="Michael Schläpfer (scm04)" HorizontalAlignment="Center" />

    <Button Content="STEMPELN" Grid.Row="1" 
            FontSize="20" Padding="20" Cursor="Hand"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Command="{Binding DoClockingCommand}"/>

    <Grid Grid.Row="2">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="40" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>
      
      <TextBlock Grid.Row="0" Grid.Column="0" Text="Aktuelle Arbeitszeit:" TextAlignment="Right" />
      <TextBlock Grid.Row="0" Grid.Column="2" Text="{Binding CurrentWorkTime, Mode=OneWay, StringFormat=\{0:h\\:mm\}}" />
      
      <TextBlock Grid.Row="1" Grid.Column="0" Text="Jahressaldo:" TextAlignment="Right" />
      <TextBlock Grid.Row="1" Grid.Column="2" Text="{Binding CurrentYearSaldo, Mode=OneWay, StringFormat=\{0:h\\:mm\}}" />

      <TextBlock Grid.Row="2" Grid.Column="0" Text="Kompletter Saldo:" TextAlignment="Right" />
      <TextBlock Grid.Row="2" Grid.Column="2" Text="{Binding CurrentTotalSaldo, Mode=OneWay, StringFormat=\{0:h\\:mm\}}" />
    </Grid>
  </Grid>
</UserControl>
